TODO List for Quiz Portal:
    -view all current employees
    -admin should be able to create quizzes
    -Add a due date for each quiz and if the date is surpassed the employee receives a 0 or whatever the admin decides.
    -Let admin assign employee a different role if by any chance they intially mistakenly assign the wrong role.
    -Create a timer for the quizzes.
    -Add security
        -Login Credentials.
        -Quiz answers should not be accessed through session storage in the console. 

    when user submits quiz only make the pop up close button show up when the user gets added to completed by list


